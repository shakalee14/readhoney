extends layouts/layout

block content
  h1= title
  h1 Booklisting 

  a(href='/') Home

  ul
    for book in books
      li 
        a(href='/book/:id'+book.title) #{book.title} 
      li By #{book.author}
      br
                




